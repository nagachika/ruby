r67620 の GC.compact の追加修正で gc_mark_maybe_() という関数で渡されたオブジェ
クトの型が T_MOVED と T_ZOMBIE の時は gc_pin() を呼んで移動しないように指定する
ようにしています。 T_MOVED はそこにあったオブジェクトが移動したって意味なので当
然ですね。 T_ZOMBIE は finalizer 待ちのオブジェクトでもうすぐ消えるから、かな?
