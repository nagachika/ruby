r54985 で configure でチェックする組み込み関数を __noreturn__ のようにアンダー
スコア2つではさんだ名前でチェックするようにしたため、定義されるマクロ名にも影響
してしまっていたので RUBY_FUNC_ATTRIBUTE() で明示的に元のマクロ名を指定して定義
させるようにしています。また拡張ライブラリ fiddle が FUNC_STDCALL の定義の有無
で Fiddle::Handle#sym の実装を分岐していたのでそのチェックのためのテストを追加
しています。 [ruby-core:75494] [Bug #12377]
