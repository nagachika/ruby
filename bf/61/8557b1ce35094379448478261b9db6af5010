test/ruby/test_jit.rb で環境変数 RUBY_FORCE_TEST_JIT がセットされていたら常に
MJIT のテストを実行するようにして、.travis.yml でこの変数をセットしておくように
しています。 r62310 で実際に MJIT が使えないと skip するようにしたので、そもそ
も MJIT がうまく動いてないとテストも実行されないという状態だったので CI では強
制的にチェックするようにするためと思われます。
