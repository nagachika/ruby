オブジェクトがマークされているかどうかを判定する rb_objspace_marked_object_p()
という API を追加しています。内容はシンプルですがコメントによると mark フェーズ
の直後でないと正しく判定できず sweep が行なわれていると判定できなくなるそうです
。追加されただけでとりあえず使ってはいないようです。 gc.h に宣言が追加されてい
るから外部の拡張ライブラリからは使えないでしょうし、objspace 用かなぁ。
