現在通常のコンパイルでは Float の一部の値は flonum といって即値(VALUE 型に値が
埋め込まれるもの)で表現されていて、0.0 も即値なのですが USE_FLONUM というマクロ
で off にもでき(そうなのか)、この時 RFLOAT_0 というマクロが毎回 DBL2NUM() で値
を変換するようになっていたので、USE_FLONUM をチェックして未使用時には RFLOAT_0
を static 変数として定義するようにしています。
