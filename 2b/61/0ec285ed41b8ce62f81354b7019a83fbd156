64838 の追加修正。 joke 命令である bitblt 命令を insns.def から削除しています。
えー、なぜこれが、と思ったらコミットログに説明があって、MinGW で VM 命令が 200
を超えると? 拡張ライブラリのビルドがこけるそうなので、ひとまず不要な bitblt を
削除して命令数を抑えることで対処しているみたいです。命令数 200 もあったっけ、と
思ったんですが trace つきの命令が別カウントになってるので単純に倍になるんですね
(それでも 100 も命令あったんだ、という感じですが)。
