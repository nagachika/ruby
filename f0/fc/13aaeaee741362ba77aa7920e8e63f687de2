r45805 の Math.atan2 の2つの引数に Float::INFINITY を渡した時に C99 の atan(3)
の挙動をするようにした変更で atan(3) が C99 準拠の挙動をするかどうかの判定が不
完全で mingw でテストが失敗していたので、configure で実際に atan(3) を呼んでみ
てその結果で判定するようにしています。 [ruby-core:62536] [Bug #9831]
