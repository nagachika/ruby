rb_yield_block() というブロック呼び出し時にブロック引数をチェックして渡されてい
たら NotImplemented 例外を発生させる関数を追加して rb_yield_block() からブロッ
ク起動する時はこれを使ってブロック引数が渡されてないかチェックするようにしてい
ます。Ruby スクリプト上の yield は現状文法的にブロック引数が渡せないので、拡張
ライブラリから利用した場合のためのチェックだと思います。
