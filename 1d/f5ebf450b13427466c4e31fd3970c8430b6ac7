Windows でマシンスタックのオーバフローの検出のために SAVE_ROOT_JMPBUF() の文の
前後をブロックで囲んで __try と __except という VC のコンパイラ拡張の記法(?)で
かこんで、例外が発生した時に対応できるようにしているそうです。ただ mingw-gcc で
は __try/__except がサポートされていないことなど制限もあるみたいで、 Windows で
のスタックオーバフロー検出はなかなか難しい状況だそうです。
