OpenSSL::PKey::RSA の鍵生成で BN_set_bit() でのビットのセットが不十分で下位
4bit (sizeof(int) bit) しか利用されていなかったため不十分な鍵を生成するようにな
ってしまっていたようです。
これは r33155 で鍵生成時の GVL 解放を入れたときのもので、1.9.3 や 1.9.2 のブラ
ンチには入っておらずリリース版には問題ありません。2011-09-01 以降の trunk 版で
、CRuby の openssl を利用して RSA鍵生成をしていたら、その鍵は使いものにならない
ので作り直しが必要です。ご注意ください。
