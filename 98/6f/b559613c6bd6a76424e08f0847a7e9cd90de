標準添付ライブラリ forwardable.rb で動的に生成するメソッドが args と block が引
数として受け取るようになっているため、accessor に args や block があるとメソッ
ドのほうでなく引数(ローカル変数)のほうを参照してしまう不具合を修正しています。
accessor の呼び出しにかっこをつけて明示的にメソッド呼び出しとして parse される
ようにしています。 [ruby-core:72579] [Bug #11916]
