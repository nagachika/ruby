r47347 で導入された make update-coverage のコマンドの不要な cd コマンドを削って
います。コメントをみると "never executed line" とあって、確かによくみるとその前
の git コマンドが exec 付きになってるので、ここでシェル(多分)のプロセスが git
のプロセスに変化してしまうのでその後が処理されないんですね。なぜ exec がいるん
だろう…
