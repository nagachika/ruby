r60763 の trace 命令の削除と trace 用の命令の動的な書き換えで、trace を無効にし
た時に trace_ の prefix つきの命令を元に戻すのを一度に行うのではなくて、その命
令の実行時に vm_trace() が呼ばれた時に戻すようにしています。なるほどー、これな
ら必要になるまで書き戻す処理が遅延できますし、vm_trace() でやるってことは一度戻
したらもう trace の処理は実行されないので余計なオーバーヘッドも増えないわけです
ね。
