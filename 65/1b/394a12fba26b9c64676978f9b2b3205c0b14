setup_exception() で例外オブジェクトを文字列化する時に Ruby のメソッドを呼ぶの
に rb_thread_t::errinfo を一時的に Qnil にクリアしておいて、そこで errinfo が上
書きされた時にそなえて後で復旧させるようにしています。 -d オプションをつけて例
外発生時のメッセージを出力するようにした時に SEGV することがあったそうです。
[ruby-core:61091] [Bug #9568]
