AIX では setgid(2) で一般ユーザが gid を変更できてしまうため、IO.popen で gid
オプションでユーザグループを変更しつつ ruby の -e オプションを使う子プロセスを
起動するテストで gid が変更されてしまい、-e オプションは setgid された状態では
使えないそうなので(知らなかった…)エラーになるのでこのテストを AIX では skip し
ています。
