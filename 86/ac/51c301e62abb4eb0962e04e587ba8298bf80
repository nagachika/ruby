rubygems で RUBY_ENGINE という定数の defined? での存在チェックしていたところは常にこの定数はあるものとして扱うようにしています。またバージョンを得るのに常に RUBY_ENGINE_VERSION という定数を参照するようにしています(これまでは JRUBY_VERSION みたいな実装毎の定数を参照していましたが、実際には外の実装はこのような定数を用意していないため)。
