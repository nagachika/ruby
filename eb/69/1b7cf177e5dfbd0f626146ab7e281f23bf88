configure の --with-valgrind というオプションのデフォルトが on になっています。
このオプションは valgrind でメモリチェックをする時に、GC ではポインタかもしれな
い値がRubyが管理しているヒープの中に入るかどうか判定するところで本来ポインタで
ないものをポインタっぽく扱うために memcheck が誤検出してエラーを沢山報告してし
まうので、valgrind にそれを無視させる命令を埋め込むようにするというもののようで
す。
