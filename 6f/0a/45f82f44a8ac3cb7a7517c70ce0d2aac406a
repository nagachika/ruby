次は gc.c の rb_raw_iseq_info() で行番号を FIX2INT に渡す前に 0 の可能性がある
ので(Fixnum なら最下位ビットが立っているはず)その時は 0 にするようにしているの
と、vm_core.h の vm_block_iseq() で引数の block が NULL の場合に SEGV の恐れが
あったのでそれも修正しています。コメントでは後者しか触れてないのでなんとなく分
けるつもりで1つのコミットに混ざったっぽいですね。 [ruby-dev:50407] [Bug #14270]
