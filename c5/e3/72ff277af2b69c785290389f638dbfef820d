キーワードrest引数がない時に未知のキーワード引数が渡された時の ArgumentError の
メッセージに不要なキーワードのリストを表示するようにしています。
この keys の String はこの関数内で生成しているので RB_GC_GUARD() をどこかに入れ
ておいたほうが良さそうですね。しかし rb_raise() は NORETURN として宣言されてる
ので後ろに入れても効かない可能性もあるので悩ましいです。
