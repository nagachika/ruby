Ractor の ractor_id の型を rb_serial_t にして 64 bit 化しています。これにより atomic な加算ができないため mutex のロックで採番するようにしています。 32bit でオーバーフローして同じ ractor_id が再利用されて採番されてしまうと異なる Ractor が同一視されて分離のチェックなどが効かなくなり、32bit だと実際に起こりうるのでこれを避けるためみたいです。
