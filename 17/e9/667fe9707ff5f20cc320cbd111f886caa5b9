Rational のパースで read_num() という関数で割る数(10の累乗)を引数のポインタに書
き戻してたのを、指数だけ指定するようにして不要に大きな Bignum を生成しなくてす
むようにしています。また極端に絶対値小さい浮動小数点数表現の文字列(Float にした
時に 0 になるようなの)を Rational() で変換した時に例外になってたのを 0 になるよ
うにしています。
