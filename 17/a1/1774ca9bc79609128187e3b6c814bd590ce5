Thread.async_interrupt_timing で :defer で割り込みを遅延している時に return や
break でそのブロックから抜けようとすると、抜けた後に例外が発生する(非同期例外な
のでどこで起きるかは通常は不定なのですが、Thread.current.raise でカレントスレッ
ドに対して例外を投げた時は、例外が発生しているはずなのに return/break でメソッ
ド/ブロックを抜けた後に例外が発生してしまっていたので、制御フローの移動の前に割
り込みをチェックするようにしています。
