正規表現のマッチの引数を String に変換するところで rb_check_string_type() を利
用して例外の発生をまかせるようにしています。 [ruby-core:50733] [Bug #7539]
ちなみに rb_convert_type() は変換しようとして失敗したら(メソッドが未定義だった
ら)例外が発生して、rb_check_convert_type() は Qnil を返すというのは、名前の印象
と逆のような気がしますね。いや、チェックしてから呼ぶぞ、みたいなニュアンスなの
か。ううむ。
