Windows で OneDrive のドライブ配下のファイルを操作(そこのスクリプトファイルを実
行)すると SEGV するという不具合修正。 Windows での link を実現する Reparse
Point というもの(そういえばそういうの前も読んだな…)のタイプが増えてて、対応で
きない時はエラーにするようにしているみたいです。 [ruby-core:83539] [Bug #14047]
