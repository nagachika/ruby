部分文字列を作る時に共有文字列オブジェクトを生成する場合、文字列の途中までの部
分文字列はこれまで NUL-terminated にするために共有できなかったのですが、それも
可能にしています。このため RSTRING_PTR() が NUL-terminated とは限らないケースが
実際に発生するようになっています、これはかなり影響デカい。と思ったら後でやっぱ
り当面はやめるようにしているみたいです。
