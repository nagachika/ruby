mjit.c や thread_pthread.c に追加された MJIT 用関数で関数ポインタを返す関数の戻
り値を void * として宣言していたのを mjit_func_t など明示的に型を指定するように
しています。 void * と関数ポインタの間の暗黙の cast はダメとのこと。また NULL
も void* にキャストされてるので 0 を使うようにしています。NULL もダメなのかー。
