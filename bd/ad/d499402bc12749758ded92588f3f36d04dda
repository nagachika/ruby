psych で 1.9.3 から追加されている Time#strftime の "%:z" という指示子を利用した
メソッドを 1.9.2 以前でも動作するように RUBY_VERSION で分岐して定義を分けていま
す。psych は独立した rubygems パッケージもあるので共通化しているのでしょう。ま
た Regexp::NOENCODING という定数も 1.9.3 から導入された(r30765)ものなので、定義
されていなかったら psych で独自に定義するようにしています。
