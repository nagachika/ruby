カレントディレクトリを得る関数 rb_dir_getwd_ospath() はファイルパス操作で頻繁に呼ばれるので、getcwd(3) を直接呼んでその結果とキャッシュしておいた文字列オブジェクトのバッファで変更がなければキャッシュしておいた文字列オブジェクトを返すようにしています。しかしこれ同じオブジェクトを返すので破壊的変更されたら……と思ったけど rb_str_freeze() しているので一応大丈夫か。あーでもこれまで freeze していない文字列を返していたので非互換にはなりますね。
