r43003 の IO.select の rdoc コメントに追加して、IO.select は readability の確認
のためにバッファの内容をチェックしてバッファに内容があればシステムコールを呼ば
ずにすぐに返りますが、この挙動は引数に渡されたのが IO オブジェクトである時だけ
で、to_io で変換できる IO-like なオブジェクトの場合はバッファのチェックはされな
いことを追記しています。
