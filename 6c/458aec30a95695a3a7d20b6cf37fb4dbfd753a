vm_malloc_increase() で malloc() によるメモリ確保の容量が増えてきたために GC を
実行する時に gc_rest_sweep() で Lazy Sweep で途中の sweep 処理を完了させて空き
容量を作ってから再度チェックするようにしています。Lazy Sweep では解放できる予定
だけどまだ free() されないメモリ領域が存在するので、本来なら不要な GC が発生し
やすくなっていたのでそれを防ぐためでしょう。
