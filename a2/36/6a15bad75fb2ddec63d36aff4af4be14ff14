r67586 のパターンマッチの文法追加では experimental な機能だということを警告で出
力しているため、テストでこの警告が大量に出てつらいということで、テストクラス定
義部分全体をヒアドキュメントにして、$VERBOSE を nil にした上で eval するという
方法で警告抑制するようにしています。そうか parse 時点で警告が出るので実行時に
$VERBOSE をいじっても抑制できないわけか。
