拡張ライブラリ openssl の string2hex() という関数で buf_len を2倍してからサイズ
チェックしていたところを、整数のオーバフローの結果が未定義であることを考慮して
最初にサイズをチェックしてから2倍するようにしています。コンパイラオプションによ
っては警告が出るみたいです。 [ruby-core:51711] [Bug #7744] https://github.com/
ruby/ruby/pull/242
