rb_io_open() および rb_io_open_generic() の引数に klass を追加して、receiver が
IO でなかった時に "|cmd" のような pipe 経由のコマンド起動を含むものが呼ばれたら
警告を出力するようにしています。つまり File.open("|cmd") や File.write("|cmd")
は IO のクラスメソッドを継承したものを呼んでいるのでコマンドを起動してしまうの
ですが、File の場合はコマンド起動しないほうが望ましいのでは? ということでとりあ
えず警告を出すようにしているようです。
