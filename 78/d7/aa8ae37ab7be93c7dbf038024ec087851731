r44133 の例外発生時の control frame の pop と例外オブジェクト生成の順番の入れか
えを revert しています。 [Bug #8886] に理由が書いてあるということですが、例外オ
ブジェクトを raise の frame で生成するとその backtrace に raise メソッドが含ま
れてしまうためだそうです。例外が raise メソッドで発生したことになる(ある意味そ
れは正しいわけですが、それだと嬉しくないので呼び元の情報が入るほうがいい)ので、
やはり control frame を pop してから例外を生成するようにしています。
