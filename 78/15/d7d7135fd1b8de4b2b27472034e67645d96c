r64355 で謎の IO.select を追加して timer を抑制して対処していた、timer thread
削除を入れたら通らなくなった fiddle のテストですが、これは MJIT とシグナル処理
の変更の関連があるみたいで、r64355 で --jit-wait オプションを指定した実行では逆
にハングアップしてしまうみたいなので、とりあえず MJIT が有効だったら skip する
ようにしています。
