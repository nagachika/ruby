vm_call_iseq_setup_normal() において cfp->sp を進めるのを vm_push_frame() の後
から前へ移動しています。コミットログによるとパフォーマンスのためということです
が、vm_push_frame() 内でここの sp が参照されるのかなと思ったらそうでもなさそう
なので、はてなんでだろう。関数の末尾を関数呼び出しにすることで最適化されやすく
するとかでしょうか(といっても return Qundef があるので違う気もしますが)。
