拡張ライブラリ oepnssl で OpenSSL 3.0 以降を利用している時に OpenSSL::PKey::{DH,DSA,EC,RSA} などの鍵のクラスの initialize を無引数で呼ぶのは受け付けず ArgumentError 例外を発生させるようにしています。鍵オブジェクトが immutable になったのでインスタンスを作っておいて後で中身をセットするという使いかたができなくなってるので、より速く気がつけるようにということですかね。
