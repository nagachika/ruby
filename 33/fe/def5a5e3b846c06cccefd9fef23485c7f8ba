r38340 の一部で rb_method_type_t の enum の最後に VM_METHOD_TYPE__MAX を置いた
のを revert しています。やっぱりこれは最後の要素を固定することで要素を追加する
たびにカンマを追加しなくていいようにするためだったんですね。 rb_method_type_t
は数値ではないので ???_MAX という名前は誤解をまねくということで revert していま
す。
