RGENGC_OLD_NEWOBJ_CHECK というマクロを導入してこれを 1 以上にしたら newobj_of()
でときどき old object として新しいオブジェクトを返すというデバッグ用の処理を入
れています。NEWOBJ() した直後の操作の Write Barrier 漏れなどを検出するためだと
思います。なるほどー failmalloc (メモリ確保のエラーチェック不足を検出するために
たまに malloc を失敗させるライブラリ)みたいなもんですね。
