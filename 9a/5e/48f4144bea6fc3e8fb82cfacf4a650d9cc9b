newobj_of() で cfp->sp のチェックをしていた gc_validate_pc() という assertion 用の関数で引数に確保したオブジェクトの VALUE を渡して、T_IMEMO 型のオブジェクトの時はこのチェックをスキップするようにしています。ZJIT から内部的に使うための一時バッファとして T_IMEMO 型オブジェクトを確保する時のこのチェックにひっかかってしまうことがあったようです。
