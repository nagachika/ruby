またしても __builtin_setjmp()/__builtin_longjmp() の検出で今度は clang 3.3 と
3.4 の不具合を回避しているそうです。詳細はわかりませんが __builtin_setjmp() の
戻り値を使わないようにしているようです。また r45508 での jmp_buf のかわりに
(void**) を使うために追加してたキャストが (void *) だったので (void**) に修正し
ています。
