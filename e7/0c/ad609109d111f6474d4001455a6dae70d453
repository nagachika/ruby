parse.y の heredoc_identifier() という関数の変数宣言のコメントにも term_len と
いう変数について説明を追加しています。それによると term_len は <<"END" みたいな
ヒアドキュメントのマーカーのうち、マーカー文字列部分(この例だと END)を除いた残
りの部分の長さ(つまり <<" と閉じる " でこの場合は4)らしい。なるほど言われないと
わからない。
