io.h の RB_IO_BUFFER_INIT()、MakeOpenFile()、RB_IO_FPTR_NEW() などのマクロは内
部実装をさらけ出しすぎということで io.c に移動して、かわりに関数として
rb_io_make_open_file() を追加しています(MakeOpenFile() の代替)。マクロを利用す
るとコンパイルエラーになるようにしています。
しかしこういうのって defined でチェックできなくて複数バージョン対応で困ったりし
ないのかなぁ。と思ったけどこのマクロそもそもいらないか普通。
