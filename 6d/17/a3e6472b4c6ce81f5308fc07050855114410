ObjectSpace.count_objects で Hash を返す時にそのキーとなる Symbol が初回の呼び出し時に生成される時に中間オブジェクトとして配列が確保されることがあり、ObjectSpace.count_objects の呼び出し自身で T_ARRAY のオブジェクト数が増える(ので2回連続で呼ぶと結果が変わる)という副作用があってテストがそれで失敗していたので、事前にキーとなる Symbol の生成をしておいて初回にカウントする前に Symbol 生成による影響を加味するようにしています。
