RubyGems の最新版マージ。 gem install -g で依存関係を記述したファイルを指定して
インストールした時に対象のファイル(Gemfile や gem.deps.rb)が存在しなかった時に
例外が発生するようにしています。
なるほど、さっきの疑問の答は `gem install -g` とすると Gemfile などを読んでくれ
るというものだったようです。と思ったら r43299の解説で自分で書いてましたね。
