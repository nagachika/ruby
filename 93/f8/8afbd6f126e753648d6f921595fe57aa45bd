clone_method() でメソッド定義をコピーする時に、rb_vm_rewrite_cref() で cref の
情報を書きかえる際に古いクラスを me->klass を参照するのをやめて、明示的にコピー
元のクラスを渡すようにしています。んー me->klass が old_class と異なっていたら
それはそのままでいいってことなんですかね。それとも実際にはこれで挙動が変更する
ことはないはず(me->klass は old_class のはず)なのかな。難しい。
