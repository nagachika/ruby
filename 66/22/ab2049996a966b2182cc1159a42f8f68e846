gc.c の GC のプロファイル情報の収集の変更。 GC 時間として mark 時間と sweep 時
間とその他(?)を合算したものを収集するようにしています。
改名された gc_prof_sweep_timer_stop() の中で sweep_time が 0 初期化されなくなっ
ていますが、if 文の中でのみ代入されているのに GC_PROFILE_MORE_DETAIL が定義され
ているとその後で利用されているので、初期化しておいたほうがいいような気がします
。
