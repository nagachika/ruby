r49781 で導入した Enumerable#each_slice や #each_cons のサイズチェックですが、
size メソッドで取得したサイズが Fixnum の時だけ比較して、そうでなければ(つまり
サイズが Bignum 以上なら)切り詰めはしないようにしています。 NUM2ULONG() で変換
できない場合を回避しているのだと思います。まあ実際にはその場合は配列のメモリ確
保でエラーになってしまいそうですが。
