parse.y の POINTER_P() というマクロを削除しています。下位2bit が 0 じゃないかど
うかで判定するマクロでしたが、これを利用しているところで判定したいのは
DVARS_INHERIT と DVARS_TOPSCOPE との比較だけで充分だったそうなので
DVARS_TERMINAL_P() というマクロを新規導入してこっちを使うようにしています。すご
いなぁ。魔境 parse.y を開拓していく…。
