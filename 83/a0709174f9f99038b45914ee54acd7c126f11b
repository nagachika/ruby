Bignum の乗算に多倍長整数ライブラリ GMP が利用可能なら使うようにしています。
bary_mul() で GMP が使える時は Toom3 アルゴリズムの実装のかわりに GMP を使うよ
うにしています。またこのために configure で libgmp を検出するようにしています。
GMP を使わないようにする --without-gmp というオプションも configure に用意して
います。ついに GMP を使うようになったんですねー。
