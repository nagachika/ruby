dup(2) に続き dup2(2) のラッパで CLOEXEC を付与する公開API rb_cloexec_dup2() を
追加して、dup2(2) のかわりにこれを利用するようにしています。
ちょっと思ったんですけど、rb_update_max_fd() でプロセスの最大のファイルディスク
リプタをカウントする処理も rb_cloexec_(open|dup|dup2) に入れてしまってはいけな
いんでしょうか。
