require の排他のための load_lock() で同じファイルを複数のスレッドで同時にロード
しようとした時に $VERBOSE が真の時にバックトレースを出力するのプロセスの標準出
力ではなくて $stderr に格納されている IO ストリームに出力するようにしています。
$stderr に代入しても C レベルの stderr はすりかえられないので $stderr を考慮す
るようにしています。
