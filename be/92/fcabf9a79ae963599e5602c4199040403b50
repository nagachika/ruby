defs/gmake.mk で make に -n オプションが指定されていた時に gnumake_recursive と
いう変数に "+" を入れるのをやめています。これは r57514 で追加された変数で
test-all などのターゲットのコマンドの先頭に + を入れるようになってます。これな
んだろう。軽く調べたのですがわからなかった。コメントによるとこれで -n 指定時に
はテストを実行しないようにしているそうです。
