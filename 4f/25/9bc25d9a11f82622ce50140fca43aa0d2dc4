GC の default 実装で空き slot を linked list で管理(freelist)していたのを、連続した slot についてはひとつひとつをリストの要素にせず、終端まではポインタをずらすだけにする(終端のポインタは構造体の要素として保持する)ようにして最適化しています。うーむなるほど。最適化といってもベンチマークでは特に明確な改善はないみたいなので、なぜこの変更をしたのかはよくわかりませんね。
