array.c の rb_ary_new_from_values() で……えーと obj というが旧世代に昇格してい
ないことをチェックする assert() を追加しているのですが、obj というローカル変数
はないんですよね……。ary の間違い? でもそもそもコンパイルできないような気がす
るのですがコンパイルは通ってますね。はて……と思ったら assert() が NDEBUG が定
義されていないと消されるからですね。
