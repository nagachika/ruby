r51541 で導入した id をキーとした st_table の実装 id_table.c で "//" による行コ
メントが使われていたので if(0) による分岐に書き換え(コメントアウトにしなかった
のはなぜだろう)、構造体の初期化子に変数を使っていたところがあったのでそれも後か
ら代入するように修正しています。 C89 準拠のコンパイラで動くようにとのことです。
[ruby-dev:49239] [Bug #11487]
