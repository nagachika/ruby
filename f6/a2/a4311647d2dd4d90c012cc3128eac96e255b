r33984 で拡張ライブラリのソースファイルの一覧をソートするようにしたものですが、
ORIG_SRCS は basename についてソートしていて、Dir.glob した結果を basename にす
る前に sort していたのを、basename 化した後で sort するようにしています。1つの
ディレクトリの下で glob しているので先に sort しても順番は変化しませんが一応。
