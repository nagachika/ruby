r44853 で get_envparam_int() と get_envparam_double() の下限値を等号も含むよう
にしたのをやっぱりやめています。数値を atoi() や atod() で変換しているため不正
な文字列が 0 になるので、下限値 0 にしていた項目でそのような不正な変数の値を受
けとった時にも 0 のまま動いてしまうのではじくためだそうです。
ついでに r44853 の ChangeLog エントリの typo 修正もしています。
