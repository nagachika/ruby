Time.now. == "" とするとレシーバと引数をいれかえて <=> を呼び出すことをくりかえ
して無限再帰に陥ってしまう不具合を修正しています。入れ替えて比較する処理を
rb_invcmp() という関数として提供して、time_cmp() でもこれを利用するようにしてい
ます。 rb_invcmp() は rb_exec_recursive() という再帰呼び出しで同じ引数での呼び
出しをチェックする関数を使って、既に呼ばれていた時にはコールバック関数の第3引数
に Qtrue を渡すようにしています。これにより Time.now == "" は nil を返すように
なります。
