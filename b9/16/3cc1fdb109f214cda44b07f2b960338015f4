proc_options(), process_options(), ruby_process_options() などで argc == 0 で
argv == NULL のケースが考慮されていなかったのを修正しています。通常最低でも
argc == 1 で argv[0] にはプログラム自体の名前(パス)が渡されるのでそういうことは
ないのですが、execv(2) で明示的に NULL を渡されたり、組み込みの利用で関数を直接
呼ばれたりしたらありうるということで(つまりやっぱり普通はない)対応しています。
[ruby-core:49889] [Bug #7423]
