GC のイベントフック起動時に rb_execution_context_t (EC) を GC の mark 時に保存した値を使っていたのが実際にフックの処理をしている Thread のものではない可能性があったため GET_EC() で実行中の Thread の EC を取得するようにして、これがない(mmtk 実装で専用スレッドで動いている)時だけ保存しておいた EC を参照するようにしています。
