これも r41204、r41205 の続きのようです。eval_error.c で例外のメッセージを取得す
るために messege メソッドを rb_funcall() で呼んで、そこで例外が発生したら
TH_TAG_EXEC() のところで捕捉するようにしていたのを、rb_check_funcall() によって
呼べるかどうかチェックしてから呼び出すようにして、さらにその結果が String (また
は String に変換できるオブジェクト)であることを確認するようにして、極力例外が発
生しないようにしています。
