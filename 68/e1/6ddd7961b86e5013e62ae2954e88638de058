obj&.meth の記法の "&." が行頭にある時に前の行の末尾の式へのメソッド呼び出しと
して parse されるようにしています。 method chain を
obj.m1()
   .m2()
   .m3()
のように書けるのを &. でも
obj&.m1()
   &.m2()
   &.m3()
と書けるようにしています。この例だとメソッド名や引数が短いので改行する必要ない
ですけどね。
