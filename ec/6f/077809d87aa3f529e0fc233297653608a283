internal.h で文末の ";" が2重になっていたのを修正、method.h で enum の最後の要
素に数値の指定つきの要素を置く END_OF_ENUMERATION() というのを定義するかどうか
の条件に __STRICT_ANSI__ が定義されているかのチェックを追加(strict ansi では一
部だけ値を指定するのが NG なのではないかと)、vm_core.h で enum の最後の要素のあ
とに "," があったのを修正など、厳格なコンパイラでもコンパイルできるように修正し
ています。
